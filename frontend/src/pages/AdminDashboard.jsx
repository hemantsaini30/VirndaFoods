// src/pages/AdminDashboard.jsx
// Placeholder route for the admin experience (approving restaurant/driver
// applications, dispute handling). Real UI arrives in a later phase.

export default function AdminDashboard() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Admin Dashboard</h1>
      <p className="text-gray-600">Placeholder route — admin experience arrives in a later phase.</p>
    </div>
  );
}
