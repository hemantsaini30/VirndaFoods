// src/shared/money.js
//
// Placeholder for Phase 1. Real conversion/formatting helpers (e.g.
// rupeesToPaise, paiseToDisplayString) are added starting Phase 3, once
// pricing logic exists. This file exists now so the folder convention is
// established: ALL money math lives here, never inline in a service file.
//
// Reminder for every future phase: money is always stored/passed around as
// an integer number of paise (smallest currency unit), never a float.

module.exports = {};
